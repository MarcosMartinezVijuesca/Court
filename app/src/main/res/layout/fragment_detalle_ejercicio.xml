<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Imagen del ejercicio -->
        <ImageView
            android:id="@+id/iv_detalle_imagen"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_nav_ejercicios"
            android:contentDescription="@string/ejercicios_titulo"
            android:background="@color/principal" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Fila badge + estrella -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_detalle_nivel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:textColor="@color/texto_claro"
                    android:background="@drawable/bg_badge_nivel" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="1dp"
                    android:layout_weight="1" />

                <ImageButton
                    android:id="@+id/btn_favorito"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_estrella_vacia"
                    android:contentDescription="@string/favoritos_añadir" />

            </LinearLayout>

            <!-- Título -->
            <TextView
                android:id="@+id/tv_detalle_titulo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface" />

            <!-- Descripción -->
            <TextView
                android:id="@+id/tv_detalle_descripcion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:textSize="15sp"
                android:lineSpacingMultiplier="1.5"
                android:textColor="?attr/colorOnSurface"
                android:alpha="0.8" />

            <!-- Separador -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="24dp"
                android:layout_marginBottom="16dp"
                android:background="?attr/colorControlHighlight" />

            <!-- Botón ver vídeo -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_ver_video"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:text="@string/ejercicio_ver_video"
                android:textSize="15sp"
                app:backgroundTint="@color/acento"
                app:icon="@drawable/ic_nav_videos"
                app:iconGravity="start"
                app:iconPadding="8dp"
                android:visibility="gone" />

            <!-- Texto sin vídeo -->
            <TextView
                android:id="@+id/tv_sin_video"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/ejercicio_sin_video"
                android:textSize="13sp"
                android:textColor="?attr/colorOnSurface"
                android:alpha="0.5"
                android:visibility="gone" />

        </LinearLayout>
    </LinearLayout>
</ScrollView>